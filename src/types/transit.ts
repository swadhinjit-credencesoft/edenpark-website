/**
 * Transit and local guide domain entities.
 */

export interface TransitItem {
  name: string;
  dist: string;
  desc: string;
}

export interface LocalGem {
  name: string;
  dist: string;
  desc: string;
  img: string;
}
